<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Orders Dashboard</title>
    <link rel="stylesheet" href="styles.css" />
    <link rel="stylesheet" href="/css/adminNavbar.css" />
    <link rel="stylesheet" href="/css/adminPanel.css" />
    <script
      src="https://kit.fontawesome.com/9d779ebc01.js"
      crossorigin="anonymous"
    ></script>
    <style>
      .dashboard {
        padding: 20px;
        width: 100%;
      }
      .search-input {
        padding: 10px 15px 10px 40px; /* Left padding increased to avoid overlap */
        width: 300px;
        border: 1px solid #ccc;
        border-radius: 5px;
        background: url("https://cdn-icons-png.flaticon.com/512/622/622669.png")
          no-repeat 10px center;
        background-size: 20px 20px;
        outline: none;
        font-size: 16px;
        box-sizing: border-box; /* Ensures padding doesn't affect the width */
      }

      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;

        background: #fff;
        padding: 15px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
      .header input {
        padding: 10px;
        width: 300px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }
      .header .create-order {
        background: #6c63ff;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }
      .stats {
        display: flex;
        justify-content: space-between;
        margin-top: 20px;
      }
      .stats-box {
        background: #fff;
        padding: 10px 20px;
        border-radius: 10px;
        margin: 0 10px;
        text-align: center;
        display: flex;
        align-items: center;
        gap: 5px;
      }
      .orders-list {
        margin-top: 20px;
        background: #fff;
        padding: 20px;
        border-radius: 5px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
      }
      th,
      td {
        padding: 15px;
        text-align: left;
        border-bottom: 1px solid #ddd;
      }
      .pending {
        color: #ff9800;
      }
      .unfulfilled {
        color: red;
      }
      .order-details {
        position: fixed;
        top: 0;
        right: 0;
        width: 450px;
        height: 100%;
        background: #fff;
        box-shadow: -2px 0 5px rgba(0, 0, 0, 0.1);
        transition: right 0.3s ease;
        padding: 20px;
        color: #333;
        overflow-y: auto; /* This will enable vertical scrolling */
      }

      .order-details.active1 {
        right: 0;
      }
      .close-btn {
        position: absolute;
        top: 10px;
        right: 15px;
        font-size: 20px;
        cursor: pointer;
      }
      .flex-orders {
        display: flex;
      }

      /* From Uiverse.io by satyamchaudharydev */
      /* From uiverse.io by @satyamchaudharydev */
      /* removing default style of button */

      .order-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 35px;
      }

      .order-input {
        max-width: 190px;
        height: 100%;
        outline: none;
        font-size: 14px;
        font-weight: 500;

        caret-color: #f7f7f8;
        color: #333;
        padding: 7px 10px;
        border: 2px solid transparent;
        border-top-left-radius: 7px;
        border-bottom-left-radius: 7px;
        margin-right: 1px;
        transition: all 0.2s ease;
      }

      .order-input:hover {
      }

      .order-input:focus {
        border: 1px solid #fa9f59;
      }

      .order-search__btn {
        border: none;
        cursor: pointer;
        background-color: #fa9f59;
        border-top-right-radius: 7px;
        border-bottom-right-radius: 7px;
        height: 100%;
        width: 30px;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .order-search__btn:hover {
        background-color: rgba(54, 54, 56, 1);
      }

      .stats-box {
        display: flex;
        border: 1px solid #ddd;
        gap: 10px;
        min-width: 200px;
      }

      .icon-box {
        border: 1px solid #ddd;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 50px;
        width: 50px;
        border-radius: 10px;
      }

      .order-texts p {
        font-size: 15px;
        font-weight: lighter;
        color: #666;
        text-align: start;
        text-wrap: nowrap;
      }
      .order-texts {
        display: flex;
        gap: 5px;
        flex-direction: column;
        align-items: flex-start;
      }
      .order-texts h2 {
        font-size: 30px;
        color: #555;
      }

      table th {
        background-color: #fff3ea;
        color: #333;
      }
      /* From Uiverse.io by andrew-demchenk0 */
      .button {
        position: relative;
        width: 150px;
        height: 40px;
        cursor: pointer;
        display: flex;
        align-items: center;
        background-color: #f60;
        border: none;
        border-radius: 10px;
      }

      .button,
      .button__icon,
      .button__text {
        transition: all 0.3s;
      }

      .button .button__text {
        transform: translateX(30px);
        color: #fff;
        font-weight: 600;
      }

      .button .button__icon {
        position: absolute;
        transform: translateX(109px);
        height: 100%;
        width: 39px;
        background-color: rgb(222, 90, 3);
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 10px;
      }

      .button .svg {
        width: 30px;
        stroke: #fff;
      }

      .button:hover {
        background: #f60;
      }

      .button:hover .button__text {
        color: transparent;
      }

      .button:hover .button__icon {
        width: 148px;
        transform: translateX(0);
      }

      .close-btn {
        width: 25px;
        height: 25px;
        border-radius: 50%;
        background-color: #999;
        font-size: 10px;
        color: #fff;
        border: none;
      }

      .order-img {
        display: flex;
        justify-content: center;
        width: 60px;
        height: 60px;
        border: 1px solid #ddd;
        border-radius: 10px;
      }
      .order-img img {
        width: 100%;
        height: 100%;
      }

      .order-list {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 2rem;
      }

      .order-item-desc h3 {
        font-size: 15px;
        color: #555;
        font-weight: 300;
      }
      .order-item-desc p {
        font-size: 12px;
        color: #777;
      }

      .denero {
        font-size: 15px;
        color: #333;
        font-weight: 500;
      }
      .quan {
        font-size: 12px;
        color: #777;
      }

      .line {
        border: 0.5px solid #eee;
        margin-top: 2rem;
      }

      .origin-text {
        font-size: 15px;
        color: #333;
        font-weight: 500;
        margin-bottom: 15px;
      }

      .order-origin-answers p {
        font-size: 15px;
        color: #333;
        font-weight: 500;
        margin-bottom: 15px;
      }
    </style>
  </head>
  <body>
    <section class="flex-orders">
      <%- include('../includes/admin-nav.ejs') %>
      <div class="dashboard">
        <div class="header">
          <div class="order-container">
            <input
              type="text"
              name="text"
              class="order-input"
              placeholder="Search order"
            />
            <button class="order-search__btn">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                width="22"
                height="22"
              >
                <path
                  d="M18.031 16.6168L22.3137 20.8995L20.8995 22.3137L16.6168 18.031C15.0769 19.263 13.124 20 11 20C6.032 20 2 15.968 2 11C2 6.032 6.032 2 11 2C15.968 2 20 6.032 20 11C20 13.124 19.263 15.0769 18.031 16.6168ZM16.0247 15.8748C17.2475 14.6146 18 12.8956 18 11C18 7.1325 14.8675 4 11 4C7.1325 4 4 7.1325 4 11C4 14.8675 7.1325 18 11 18C12.8956 18 14.6146 17.2475 15.8748 16.0247L16.0247 15.8748Z"
                  fill="#efeff1"
                ></path>
              </svg>
            </button>
          </div>
          <button type="button" class="button">
            <span class="button__text">Add Order</span>
            <span class="button__icon">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                viewBox="0 0 24 24"
                stroke-width="2"
                stroke-linejoin="round"
                stroke-linecap="round"
                stroke="currentColor"
                height="24"
                fill="none"
                class="svg"
              >
                <line y2="19" y1="5" x2="12" x1="12"></line>
                <line y2="12" y1="12" x2="19" x1="5"></line>
              </svg>
            </span>
          </button>
        </div>

        <div class="orders-list">
          <table>
            <thead class="t-head">
              <tr>
                <th>Order</th>
                <th>Date</th>
                <th>Customer</th>
                <th>Payment</th>
                <th>Total</th>
                <th>Items</th>
                <th>Fulfillment</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody>
              <% orders.forEach(order => { %>
              <tr onclick="openOrderDetails('<%= order._id %>')">
                <td>#<%= order.orderNumber %></td>
                <td>
                  <%= new Date(order.createdAt).toLocaleDateString('en-US', {
                  year: 'numeric', month: 'short', day: 'numeric' }) %>
                </td>
                <td><%= order.user.email %></td>
                <td class="<%= order.status.toLowerCase() %>">
                  <%= order.status %>
                </td>
                <td>$<%= order.totalPrice.toFixed(2) %></td>
                <td><%= order.products.length %> items</td>
                <td class="<%= order.status.toLowerCase() %>">
                  <%= order.status %>
                </td>
                <td>...</td>
              </tr>
              <% }); %>
            </tbody>
          </table>
        </div>

        <div class="order-details" id="orderDetails" style="display: none">
          <button class="close-btn" onclick="closeOrderDetails()">x</button>
          <div class="line"></div>
          <div id="orderContent"></div>
        </div>
      </div>
    </section>

    <script>
            const orders = <%- JSON.stringify(orders) %>;

      function openOrderDetails(orderId) {
        const order = orders.find(o => o._id === orderId);
        console.log(order)
        if (order) {
          const orderContent = `
            <div>
              <div style="max-height: 300px; overflow-y: auto;">
                ${order.products.map(product => `
                  <div class="order-list">
                    <div class="order-img">
                      <img src="${product.productId.imageUrl[0]}" alt="${product.productId.name}" />
                    </div>
                    <div class="order-item-desc">
                      <h3>${product.productId.name}</h3>
                      <p>${product.productId.category}</p>
                    </div>
                    <p class="quan">${product.quantity}pcs</p>
                    <p class="denero">$${product.productId.price.toFixed(2)}</p>
                  </div>
                  <div class="line"></div>
                `).join('')}
              </div>
              <div class="order-grid" style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 2rem;">
                <div class="order-origin">
                  <p class="origin-text">Customer Name:</p>
                  <p class="origin-text">Order Number:</p>
                  <p class="origin-text">Email:</p>
                  <p class="origin-text">Order Date:</p>
                  <p class="origin-text">Phone:</p>
                </div>
                <div class="order-origin-answers">
                  <p>${order.user.email}</p>
                  <p>#${order.orderNumber}</p>
                  <p>${order.user.email}</p>
                  <p>${new Date(order.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}</p>
                  <p>${order.phone}</p>
                </div>
              </div>
              <div class="line"></div>
              <p class="origin-text" style="margin-top: 2rem">Shipping Address</p>
              <p class="origin-text">${order.address}</p>
              <div class="line"></div>
              <p class="origin-text" style="margin-top: 2rem">Payment</p>
              <div class="order-total" style="display: flex; justify-content: space-between; margin-top: 4rem;">
                <p class="origin-text" style="font-weight: 800">Total</p>
                <p class="origin-text" style="font-weight: 800">$${order.totalPrice.toFixed(2)}</p>
              </div>
            </div>
          `;
          document.getElementById('orderContent').innerHTML = orderContent;
          document.getElementById('orderDetails').style.display = 'block';
        }
      }


            function closeOrderDetails() {
              document.getElementById('orderDetails').style.display = 'none';
            }
    </script>
  </body>
</html>
